# Packages used by the desktop application on Python 3.10.
# we dont need tensorflow here, because we are not training , we are simply running the program. This much works
numpy==1.23.5
protobuf==3.20.3
mediapipe==0.10.9
opencv-contrib-python==4.8.1.78
Pillow==10.2.0
pyttsx3==2.90
pywin32==306; platform_system == "Windows"
sounddevice==0.4.6
scipy==1.10.1
SpeechRecognition==3.10.1
tqdm==4.66.1